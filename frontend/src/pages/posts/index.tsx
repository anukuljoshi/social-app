import { Container, Typography } from '@mui/material';

const PostIndex = () => {
    return (
        <Container>
            <Typography variant={"h2"}>Hello Posts</Typography>
        </Container>
    );
};

export default PostIndex;
